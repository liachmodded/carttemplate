{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "double cart",
  "description": "${description}",
  "authors": [
    "liach"
  ],
  "contact": {
    "homepage": "https://${repoOwner}.github.io/${repoName}/overview-summary.html",
    "sources": "https://github.com/${repoOwner}/${repoName}",
    "issues": "https://github.com/${repoOwner}/${repoName}/issues"
  },
  "license": "MPL-2.0",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.github.liachmodded.doublecart.DoubleCartEntry"
    ]
  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "fabricloader": "${loaderVersionLimit}",
    "minecraft": "${minecraftVersionLimit}"
  }
}
